#ifndef FOOD_CHAIN
#define FOOD_CHAIN
#include <string>
#include <utility>

namespace food_chain {
	std::string verse(uint16_t);
	std::string verses(uint16_t, uint16_t);
	std::string sing();
}
#endif // !FOOD_CHAIN
