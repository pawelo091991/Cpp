#ifndef BINARY_SEARCH_H
#define BINARY_SEARCH_H

#include <vector>
#include <cstddef>
namespace binary_search {
	std::size_t find(std::vector<int> data, std::size_t target);
}
#endif // !BINARY_SEARCH_H
